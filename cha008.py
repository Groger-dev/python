#conversor de metros para cm e mm
medida = eval(input('Insira a medida em metros: '))

cm = (medida * 100)
mm = (medida * 1000)

print('A medida em cm é: {}'.format(cm))
print('A medida em mm é: {}'.format(mm))
